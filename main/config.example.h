#pragma once

// Copy this file to config.h for the initial serial-flashed bootstrap build.
// The credentials are saved to NVS and are not included in OTA release builds.
#define WIFI_SSID "your-network"
#define WIFI_PASS "your-password"
